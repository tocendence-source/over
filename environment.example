# This file is kept for compatibility with existing documentation.
# The canonical environment template is .env.example — see that file.
#
# Short version: this site builds and serves with no environment variables,
# and no secret may ever use a VITE_* name.
